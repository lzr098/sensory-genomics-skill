{# 执行摘要 - v2 格式 #}
## 1. 执行摘要

### 1.1 样本信息

| 项目 | 内容 |
|------|------|
| 样本 ID | {{ sample_id or "未提供" }} |
| 性别 | {{ "男性" if sex == "M" else "女性" }} |
| 参考基因组 | {{ ref_genome }} |
| 分析日期 | {{ analysis_date }} |
| 分析范围 | {{ ", ".join(subsystems) }} |

### 1.2 个人特征速览

基于关键 SNP 基因型推断与基因层面变异数据，以下是个人特征的定性预测：

| 特征 | 预测结论 | 置信度 | 关键依据 |
|------|---------|--------|---------|
{% for trait in personal_traits %}
| **{{ trait.trait }}** | {{ trait.prediction }} | {{ trait.confidence }} | {{ trait.evidence[:60] }}{% if trait.evidence | length > 60 %}...{% endif %} |
{% endfor %}

> **说明**：以上预测基于已知关键 SNP 的 REF/REF 推断或 VCF 实际检出基因型，结合基因层面的功能变异评估。预测为**定性推断**，非确定性结论，建议结合实际表型观察验证。

### 1.3 核心发现速览

{% set subsys_names = {"vision": "视觉", "hearing": "听觉", "olfaction": "嗅觉", "taste": "味觉", "somatosensation": "体感/痛觉/温度觉", "pigmentation": "色素沉着", "metabolism": "代谢特征", "muscle": "肌肉特征", "hair": "毛发特征"} %}

| 感官领域 | 关键发现 | 表型影响推断 |
|---------|---------|-------------|
{% for finding in key_findings %}
| {{ subsys_names.get(finding.subsystem, finding.subsystem) }} | {{ finding.gene }}: {{ finding.level | level_badge }} | {{ finding.phenotypic_impact[:60] if finding.phenotypic_impact else "" }}... |
{% else %}
| — | 无高风险发现 | 各感官系统未检出需要关注的致病性变异 |
{% endfor %}

### 1.4 值得关注的高优先级发现

{% if key_findings %}
{% for finding in key_findings %}
- **{{ finding.gene }}** ({{ subsys_names.get(finding.subsystem, finding.subsystem) }}) — {{ finding.level | level_badge }}
  - **分子依据**：{{ finding.rationale }}
  - **表型推断**：{{ finding.phenotypic_impact }}
{% endfor %}
{% else %}
- 暂无高优先级发现。所有感官相关基因均未检出完全丧失或显著影响级别的功能改变变异。
{% endif %}

---
