{# 执行摘要 - v5 格式：合并 1.3/1.4 为统一的重点关注发现表 #}
## 1. 执行摘要

### 1.1 样本信息

| 项目 | 内容 |
|------|------|
| 样本 ID | {{ sample_id or "未提供" }} |
| 性别 | {{ "男性" if sex == "M" else "女性" }} |
| 参考基因组 | {{ ref_genome }} |
| 分析日期 | {{ analysis_date }} |
| 分析范围 | {{ ", ".join(subsystems) }} |

### 1.2 个人特征速览

基于关键 SNP 基因型推断与基因层面变异数据，以下是个人特征的定性预测：

| 特征 | 预测结论 | 置信度 | 关键依据 |
|------|---------|--------|---------|
{% for trait in personal_traits %}
| **{{ trait.trait }}** | {{ trait.prediction }} | {{ trait.confidence }} | {{ trait.evidence[:80] }}{% if trait.evidence | length > 80 %}...{% endif %} |
{% endfor %}

> **说明**：以上预测基于 VCF 实际检出基因型或 REF/REF 推断。预测为**定性推断**，非确定性结论，建议结合实际表型观察验证。

### 1.3 重点关注发现

以下基因在本样本中评估为**"部分影响"及以上**，建议优先关注：

{% set subsys_names = {"vision": "视觉", "hearing": "听觉", "olfaction": "嗅觉", "taste": "味觉", "somatosensation": "体感/痛觉/温度觉", "pigmentation": "色素沉着", "metabolism": "代谢", "muscle": "肌肉", "hair": "毛发"} %}

| 基因 | 子系统 | 评估 | 遗传模式 | 关键变异 | 支持质量 | 科普一句话 |
|------|--------|------|---------|---------|---------|-----------|
{% for finding in key_findings %}
| **{{ finding.gene }}** | {{ subsys_names.get(finding.subsystem, finding.subsystem) }} | {{ finding.level | level_badge }} | {{ finding.inheritance_pattern or "未知" }} | {{ finding.variants[:50] if finding.variants else "—" }} | {{ finding.quality_summary or "—" }} | {{ finding.phenotypic_impact[:350] if finding.phenotypic_impact else "—" }}{% if finding.phenotypic_impact | length > 350 %}...{% endif %} |
{% else %}
| — | — | — | — | — | 未检出"部分影响"及以上级别的基因变异 |
{% endfor %}

> **阅读指引**：上表为速览，每个发现的详细科普解释和分子机制见 [第 3 章](#3-感官子系统深度分析)。

---
