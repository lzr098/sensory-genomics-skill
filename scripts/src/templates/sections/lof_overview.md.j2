{# 第 2 章：功能丧失变异总览 - v5 格式 #}
## 2. 功能丧失变异总览

> **章节说明**：本节列出具有明确蛋白功能后果（移码、无义、剪接位点破坏）的变异。"功能丧失"指该变异大概率导致所编码蛋白无法正常行使功能。以下按子系统分类，突出需要关注的条目。

### 2.1 嗅觉受体 — 已知配体变异筛查

> ⚠️ **嗅觉系统冗余说明**：人类约有 400 个功能性嗅觉受体基因。单个 OR 基因变异通常不会导致完全无法感知某种气味，但可能降低对该气味分子的**敏感度**或在低浓度下**辨识度**下降。

{% if known_ligand_or_tiers and known_ligand_or_tiers.summary %}
{% set ks = known_ligand_or_tiers.summary %}

> **筛查范围**: {{ ks.screened }} 个已知配体 OR 基因  
> **检出**: {{ ks.with_variants }} 个基因有覆盖，{{ ks.with_protein_impact }} 个有蛋白影响变异  
> **经 gnomAD 频率校验**: {{ ks.downgraded_by_af }} 个为人群常见多态，不构成功能影响  
{% if ks.with_protein_impact - ks.downgraded_by_af > 0 %}
> **值得关注**: {{ ks.with_protein_impact - ks.downgraded_by_af }} 个基因的变异可能影响功能
{% else %}
> **结论**: 所有检出蛋白影响变异的基因均为人群常见多态，无功能影响。
{% endif %}

| 基因 | 配体气味 | 变异类型 | AF | 解读 |
|------|---------|---------|-----|------|
{% for entry in known_ligand_or_tiers.all_genes if entry.has_variants %}
{% set gene = entry.gene %}
{% set odor = entry.odor_zh if entry.odor_zh else entry.ligand_zh %}
{% set af = entry.max_af %}
{% set af_str = ('%.0f%%'|format(af*100)) if af is not none else 'N/A' %}
{% set note = entry.af_note if entry.af_note else '—' %}
{% if entry.tier_a_variants %}
{% for v in entry.tier_a_variants[:1] %}
| **{{ gene }}** | {{ odor[:25] if odor else '—' }} | HOM {{ v.consequence.replace('_variant','') }} | {{ af_str }} | {{ note }} |
{% endfor %}
{% elif entry.tier_b_variants %}
{% for v in entry.tier_b_variants[:1] %}
| {{ gene }} | {{ odor[:25] if odor else '—' }} | HET {{ v.consequence.replace('_variant','') }} | {{ af_str }} | {{ note }} |
{% endfor %}
{% else %}
| {{ gene }} | {{ odor[:25] if odor else '—' }} | UTR/syn | — | 非编码区变异 |
{% endif %}
{% endfor %}

> **注**: {{ ks.tier_c_count }} 个仅有同义/UTR 变异的基因未列出。{{ ks.not_covered }} 个基因不在测序覆盖范围内。
{% else %}
> 未在已知配体的 OR 基因中检出需要关注的变异。
{% endif %}

### 2.2 嗅觉受体 — 配体未知的纯合变异（经频率筛选）

{% if unknown_or_gating %}
{% set uog = unknown_or_gating %}
{% set total = uog.layer1|length + uog.layer2|length + uog.layer3|length %}
本样本中检出 **{{ total }} 个配体未知 OR 基因**的纯合蛋白影响变异。经 gnomAD 频率筛选后：

- **{{ uog.layer3|length }} 个基因**有 AF < 5% 的罕见变异 → 值得关注（见下）
- **{{ uog.layer2|length }} 个基因** AF 5-30% → 附录简表
- **{{ uog.layer1|length }} 个基因** 全部纯合变异 AF > 30% → 人群常见多态，已从报告中隐藏

{% if uog.layer3 %}
**值得关注的罕见变异**：
| 基因 | 纯合变异数 | gnomAD AF |
|------|-----------|-----------|
{% for entry in uog.layer3 %}
| **{{ entry.gene }}** | {{ entry.n_homo_variants }} | {{ ('%.2f%%'|format(entry.min_af*100)) if entry.min_af is not none else 'N/A' }} |
{% endfor %}
{% endif %}

{% if total == uog.layer1|length %}
> **结论**: 所有检出纯合蛋白影响变异的配体未知 OR 基因均为人群常见多态（AF > 30%），不构成功能影响。
{% endif %}

> 由于嗅觉系统的高度冗余（约 400 个受体协同工作），即使单个 OR 基因的罕见纯合失活通常也不会导致明显的嗅觉缺陷。详细列表见附录 G。
{% else %}
> 无数据。
{% endif %}

### 2.3 其他子系统功能丧失变异

除上述已列出的基因外，以下基因亦检出功能丧失变异，但评估为"无影响"或"可能轻微影响"：

{% if other_lof %}
| 基因 | 子系统 | 变异 | 基因型 | 评估 |
|------|--------|------|--------|------|
{% for item in other_lof %}
| {{ item.gene }} | {{ item.subsystem or "N/A" }} | {{ item.variant.chrom }}:{{ item.variant.pos }} | {{ item.variant.gt }} | 杂合/轻微，冗余系统或临床意义有限 |
{% endfor %}
{% else %}
> 未检出其他需要列出的功能丧失变异。
{% endif %}

---
