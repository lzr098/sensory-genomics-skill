{# 个性化特征板块 - v0.2.0 前置独立章节 #}
## 2. 个性化特征速览

> ⚠️ **仅供娱乐参考**：以下特征预测基于文献报道的 SNP-表型关联和基因功能推断，为**定性预测**，不用于医疗目的。感官和个人特征受基因、环境、生活方式等多因素影响。

基于关键 SNP 基因型推断与基因层面变异数据，以下是个人特征的定性预测：

| 特征 | 预测结论 | 置信度 | 一句话解读 |
|------|---------|--------|-----------|
{% for trait in (personal_traits or []) %}
| **{{ trait.trait }}** | {{ trait.prediction }} | {{ trait.confidence }} | {% if trait.trait == "瞳孔颜色" %}{{ "虹膜色素沉积模式预测" }}{% elif trait.trait == "毛发颜色" %}{{ "真黑色素/褐黑色素比例预测" }}{% elif trait.trait == "皮肤颜色" %}{{ "肤色深浅多基因综合预测" }}{% elif trait.trait == "咖啡因代谢" %}{{ "CYP1A2 酶活性快慢型预测" }}{% elif trait.trait == "酒精代谢" %}{{ "乙醛脱氢酶活性与饮酒后反应预测" }}{% elif trait.trait == "乳糖耐受" %}{{ "LCT 基因启动子区变异预测" }}{% elif trait.trait == "毛发卷曲" %}{{ "EDAR 基因功能与毛囊形态预测" }}{% elif trait.trait == "耳垢类型" %}{{ "ABCC11 基因与大汗腺分泌预测" }}{% else %}{{ trait.evidence[:60] if trait.evidence else "—" }}{% endif %} |
{% else %}
| — | — | — | 暂无个性化特征预测数据 |
{% endfor %}

> **说明**：以上预测基于 VCF 实际检出基因型或 REF/REF 推断。预测为**定性推断**，非确定性结论，建议结合实际表型观察验证。
>
> **置信度评分**：⭐⭐⭐⭐⭐ = 多基因/多SNP一致支持；⭐⭐⭐⭐ = 关键SNP VCF检出+基因评估吻合；⭐⭐⭐ = 主要依据REF/REF推断或单一位点；⭐⭐ = 数据有限或部分矛盾；⭐ = 数据严重不足。

### 2.1 全部关键性状 SNP（{{ key_snps | length }} 个）

下表列出本次分析覆盖的全部关键 SNP。基因型为 **VCF 实际检出** 的以 ✅ 标记；未检出的位点根据参考基因组推断为 REF/REF（以 📌 标记）。

| rsID | 基因 | 位置 | 基因型 | 来源 | 表型标签 | 表型描述 |
|------|------|------|--------|------|----------|----------|
{% for snp in (key_snps or []) %}
| {{ snp.rsid }} | {{ snp.gene }} | chr{{ snp.chrom }}:{{ snp.pos }} | {{ snp.inferred_genotype }} | {% if snp.found_in_vcf %}✅ VCF{% else %}📌 REF推断{% endif %} | {{ snp.phenotype_label }} | {{ snp.phenotype_description[:60] if snp.phenotype_description else "—" }}{% if snp.phenotype_description and snp.phenotype_description | length > 60 %}...{% endif %} |
{% else %}
| — | — | — | — | — | — | 无关键 SNP 数据 |
{% endfor %}

> **详细注释**：每个 SNP 的文献依据、等位基因功能及人群频率信息见 JSON 输出中的 `key_snps` 字段。

---
